{
  "name": "hello-i-am-lorem",
  "version": "2.0.0",
  "description": "Atmospheric static showcase app with name profiling, local history and GitHub Pages deployment.",
  "main": "src/scripts/app.js",
  "scripts": {
    "test": "node ./node_modules/jest/bin/jest.js",
    "test:report": "node ./node_modules/jest/bin/jest.js --json --outputFile=src/test-results.json"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^20.0.0"
  },
  "jest": {
    "testEnvironment": "jest-environment-jsdom"
  }
}
