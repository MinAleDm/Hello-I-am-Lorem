{
  "name": "thinking-ui-studio",
  "version": "2.0.0",
  "description": "Static adaptive frontend case study with explainable UI changes, local-first memory and GitHub Pages deployment.",
  "main": "src/scripts/app.js",
  "scripts": {
    "test": "node ./node_modules/jest/bin/jest.js",
    "test:report": "node ./node_modules/jest/bin/jest.js --json --outputFile=src/test-results.json"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^20.0.0"
  },
  "jest": {
    "testEnvironment": "jest-environment-jsdom"
  }
}
